# custom env variables
[ -f "$HOME/.environment_variables" ] && source "$HOME/.environment_variables"
if command -v dircolors &>/dev/null && [ -f "$HOME/.dir_colors/dircolors" ]; then
    eval "$(dircolors "$HOME/.dir_colors/dircolors")"
fi



