# Unified Shell Aliases (Zsh & Bash)

# git custom workflows
alias gcommit='git add -A && git commit'
alias gamend='git add -A && git commit --amend --no-edit'
alias gfetch='git fetch'
alias gpush='git push origin HEAD'
alias gpushf='git push --force-with-lease origin HEAD'
alias gpull='git pull --rebase origin HEAD'
alias gup='gfetch && gpull'
alias gprune='git branch --format="%(refname:short)" | grep -E -v "^(main|master)$" | xargs -r git branch -D'
alias guser-branch='git branch -m "$USER/$(git rev-parse --abbrev-ref HEAD | sed -E "s|^($USER/)+||")"'


# editor shortcuts (Neovim default with Vim fallback)
if command -v nvim >/dev/null 2>&1; then
    alias vi='nvim'
    alias vim='nvim'
    alias v='nvim'
else
    alias vi='vim'
    alias v='vim'
fi

# modern file viewing (bat with TrueColor Solarized Dark)
if command -v bat >/dev/null 2>&1; then
    alias cat='bat --theme="Solarized-Dark-TrueColor" --paging=auto'
elif command -v batcat >/dev/null 2>&1; then
    alias cat='batcat --theme="Solarized-Dark-TrueColor" --paging=auto'
    alias bat='batcat'
fi

# standard directory listing (GNU ls / BSD ls defaults)
if [ "$(uname -s)" = "Darwin" ]; then
    alias ls='ls -G'
    alias ll='ls -alFG'
    alias la='ls -AG'
    alias l='ls -CFG'
else
    alias ls='ls --color=auto'
    alias ll='ls -alF'
    alias la='ls -A'
    alias l='ls -CF'
fi

# optional modern directory listing (eza shortcuts)
if command -v eza >/dev/null 2>&1; then
    alias el='eza -la --git --group-directories-first'
    alias et='eza --tree --level=2'
    alias ea='eza -a'
fi

# golang
alias go-lint='golangci-lint run'
alias go-testall='go test ./...'
alias go-buildall='go build ./...'

# terraform
alias tf='terraform'

# Aux
alias yaml-lint='yamllint -d relaxed'
if ! command -v fd >/dev/null 2>&1 && command -v fdfind >/dev/null 2>&1; then
    alias fd='fdfind'
fi

