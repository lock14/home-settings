#!/bin/bash
# Standalone JDK installer for Ubuntu
# Delegates to ubuntu/install-jdk.sh

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/install-jdk.sh" "$@"
